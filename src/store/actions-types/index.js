const articleData = {
  SET_SLUG: 'SET_SLUG',
  DELETE_ARTICLE: 'DELETE_ARTICLE',
};

const authentication = {
  LOG_OUT: 'LOG_OUT',
  UPDATE_USER: 'UPDATE_USER',
};

const actionTypes = {
  articleData,
  authentication,
};

export default actionTypes;
