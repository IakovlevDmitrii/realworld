const article = {
  SET_SLUG: 'SET_SLUG',
};

const authentication = {
  LOG_OUT: 'LOG_OUT',
  UPDATE_USER: 'UPDATE_USER',
};

const actionTypes = {
  article,
  authentication,
};

export default actionTypes;
